import { Banana, File, Folder, Loader2, LucideProps } from 'lucide-react';

export type Icon = LucideProps;

export const Icons = {
  folder: Folder,
  file: File,
  spinner: Loader2,
  brand: Banana,
};
